This repository should be in the home directory: `~/dotfiles`

Then use `stow zsh`, etc to create symlinks for the dotfiles you want.

The logiops file goes somewhere else:

`sudo cp logid.cfg /etc/logid.cfg`
